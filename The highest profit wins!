The highest profit wins
function minMax(arr){
let min = 0;
let max = 0;
  for(i=0;i<arr.length;i++){
    min = (min===0)?arr[i]:Math.min(min,arr[i]);
    max = (max===0)?arr[i]:Math.max(max,arr[i]);
  }
  return [min,max];
}!
