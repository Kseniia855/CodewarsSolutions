Sum of Odd Cubed Numbers.
function cubeOdd(arr){
let sum = 0;
for(let i = 0; i < arr.length; i++){
  if (isNaN(arr[i])) {
          return undefined;}
  else{
       if(arr[i] % 2 !== 0){
          sum = sum + arr[i]**3;
       }
  }
}
return sum;
}
